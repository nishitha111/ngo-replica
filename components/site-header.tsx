"use client";
import Link from "next/link";import {Menu,X,Heart} from "lucide-react";import {useState} from "react";import {DonationModal} from "./donation-modal";
const links=[['About','/about'],['Meal Programs','/programs/morning-meals'],['Food Safety','/food-safety'],['Mothers','/mothers'],['Schools','/schools'],['Articles','/articles'],['Volunteer','/volunteer'],['Contact','/contact']];
export function SiteHeader(){const[open,setOpen]=useState(false);return <><header className="sticky top-0 z-40 border-b border-forest/10 bg-cream/95 backdrop-blur"><div className="container flex h-20 items-center justify-between"><Link href="/" className="flex items-center gap-2 text-lg font-bold text-forest"><span className="grid h-10 w-10 place-items-center rounded-full bg-forest text-gold"><Heart size={18}/></span><span className="display">Sahaara Foundation</span></Link><nav className="hidden items-center gap-5 lg:flex">{links.map(([label,href])=><Link key={href} href={href} className="text-sm font-medium hover:text-forest">{label}</Link>)}<DonationModal trigger="Donate now"/></nav><button onClick={()=>setOpen(!open)} className="lg:hidden text-forest" aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="container flex flex-col gap-4 border-t border-forest/10 py-5 lg:hidden">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href}>{label}</Link>)}<DonationModal trigger="Donate now"/></nav>}</header></>}
